# Churrascaria
This project simulates a Brazilian steakhouse order system using basic Java OOP. It has three classes: Produto, which represents a menu item; Comandas, which manages a table's tab, validates items, calculates the total and closes the bill; and Main, which runs the program.
